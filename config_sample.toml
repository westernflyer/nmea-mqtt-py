# Configuration file for nmea-mqtt-py
#
#    DO NOT EDIT THIS FILE!
#
#    Instead, copy it to config.toml, then edit that.
#

# Set to 1 for debug messages, set to 2 for even more messages
# This can be overridden with the NMEA_MQTT_DEBUG environment variable.
DEBUG = 0

# Vessel related
MMSI = 368323170

# NMEA socket configuration
[NMEA_OPTIONS]
NMEA_SOCKETS = [
    ["localhost", 10110],
    ["localhost", 10111],
]
NMEA_TIMEOUT = 20
NMEA_RETRY_WAIT = 60

# MQTT configuration.
[MQTT_OPTIONS]
MQTT_BROKER = "localhost"
MQTT_PORT = 1883
MQTT_TOPIC_PREFIX = "nmea"
MQTT_USERNAME = ""
MQTT_PASSWORD = ""

# InfluxDB V3 configuration.
# HOST_URL is the URL of your InfluxDB instance (e.g. https://us-east-1-1.aws.cloud2.influxdata.com).
#   Either set it here, or set environment variable INFLUXDB3_HOST_URL
# TOKEN is your API token. Either set it here, or set environment variable INFLUXDB3_AUTH_TOKEN.
# DATABASE_NAME is the name of your database/bucket. Either set it here, or set
#   environment variable INFLUXDB3_DATABASE_NAME
# [INFLUXDB]
# HOST_URL = "http://localhost:8181"
# AUTH_TOKEN = "your-token"
# DATABASE_NAME = "your-database"

# How often to publish in milliseconds. If an address field is not listed below,
# it will not get published.
[MQTT_PUBLISH_INTERVALS]
# GPGGA = 10000  # Lat, lon plus quality indicators.
GPGLL = 10000    # Lat, lon
# GPRMC = 10000  # Lat, lon, SOG, COG
GPVTG = 10000    # COG & SOG
HEHDT = 10000    # Heading - True
IIMDA = 10000    # Met data. Baro, temperature, true wind
IIRSA = 10000    # Rudder Sensor Angle
SDDPT = 10000    # Depth of water
TIROT = 10000    # Rate of turn
WIMWV = 10000    # Relative wind speed and angle - Airmar
FTMWV = 10000    # Relative wind speed and angle - FT602
